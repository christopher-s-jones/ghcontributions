package reporting

// The interval used to poll the Github GraphQL API for contributions
const PollingIntervalInMinutes = 2

// The default first contribution year
const DefaultFirstContributionYear = 2000
